{
  "name": "moil-manganese-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "AI/ML & Space Technology powered system for manganese reserve identification and production shortfall prediction for MOIL Ltd.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && venv/bin/uvicorn app.main:app --reload --port 8000",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint"
  }
}
